# ─────────────────────────────────────────────────────────────
# Variables Portainer — PRODUCTION derrière le domaine sahel-academy.com
# (le nginx-proxy-manager du serveur redirige les domaines vers les conteneurs)
#
#   sahel-academy.com       → conteneur sahel_academy_web     (port interne 3000)
#   app.sahel-academy.com   → conteneur sahel_academy_backend (port interne 3001)  [l'API]
#
# ⚠️ Les NEXT_PUBLIC_* sont FIGÉES AU BUILD : après les avoir changées, il faut
#    REDÉPLOYER EN RECONSTRUISANT le site (rebuild).
# ─────────────────────────────────────────────────────────────

# --- Base de données Neon (à renseigner) ---
DATABASE_URL=postgresql://user:password@host.neon.tech/db?sslmode=require

# --- Auth ---
BETTER_AUTH_SECRET=change-moi-secret-long-aleatoire
BETTER_AUTH_URL=https://app.sahel-academy.com
# Cookie partagé entre le site et l'API (les deux sous *.sahel-academy.com)
COOKIE_DOMAIN=.sahel-academy.com

# --- URLs du front (CORS + redirections côté backend) ---
FRONTEND_URL=https://sahel-academy.com
WEB_URL=https://sahel-academy.com
ADMIN_URL=https://sahel-academy.com
VERIF_URL=https://sahel-academy.com

# --- Build args du site Next.js (figés au build → rebuild obligatoire) ---
# Appels API DEPUIS LE NAVIGATEUR → URL publique de l'API (app.)
NEXT_PUBLIC_API_URL=https://app.sahel-academy.com/api/v1
NEXT_PUBLIC_VERIF_URL=https://sahel-academy.com

# --- Rendu serveur (SSR) du site : appelle le backend EN INTERNE par son nom ---
# (indispensable : le conteneur web n'a pas d'egress Internet sur common_net)
INTERNAL_API_URL=http://sahel_academy_backend:3001/api/v1

# --- OAuth Google (login) ---
# Dans Google Cloud Console, URI de redirection autorisée :
#   https://app.sahel-academy.com/api/v1/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Intégrations optionnelles ---
BUNNY_ACCESS_KEY=
BUNNY_STORAGE_ZONE=
BUNNY_STORAGE_ENDPOINT=
BUNNY_CDN_DOMAIN=
FIREBASE_ADMIN_CREDENTIALS_BASE64=
