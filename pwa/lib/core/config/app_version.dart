/// Constantes de version et d'application pour Sahel Academy.
class AppVersion {
  AppVersion._();

  /// Numéro de version sémantique de l'application
  static const String version = '1.1.0';

  /// Numéro de build
  static const int buildNumber = 103;

  /// Libellé complet à afficher dans l'interface
  static const String label = 'v$version (Build $buildNumber)';
}
